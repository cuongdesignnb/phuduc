Order mode validates order-item ownership and snapshots product/customer fields from the selected order.
Manual mode requires product, customer name and normalized Vietnamese phone.
Remote order picker is bounded to 20; no fixed first-100 order load remains.
Order-item lookup returns all scoped items: 51-item fixture returned item 51 without truncation.
Cross-order item selection returns 422 with: Sản phẩm đã chọn không thuộc đơn hàng này.
Voided warranty update is rejected; repeated void keeps the first reason and does not mutate the terminal record.
